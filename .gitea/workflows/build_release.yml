name: build/release

on:
  push:

jobs:
  post:
    needs: pre
    runs-on: ubuntu-latest
    steps:
      - run: ${{ tojson(needs) }}
        shell: cat {0}
  pre:
    runs-on: ubuntu-latest
    steps:
      - run: echo "out=Test2" > "$GITHUB_ENV"
        id: out
    outputs:
      out: ${{ env.out }}